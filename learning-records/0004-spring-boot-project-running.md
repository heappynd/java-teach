# Spring Boot 项目创建并运行

用户成功使用 Spring Initializr 创建了第一个 Spring Boot 项目，在 IntelliJ IDEA 中配置了 JDK 21 并成功运行。项目能正常启动，Tomcat 监听 8080 端口。访问 localhost:8080 出现 Whitelabel Error Page（正常，因为还没有接口）。用户已具备编写第一个 REST API 的条件。
