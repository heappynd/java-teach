# 第一个 REST API 完成

用户成功编写并运行了三个 REST API 接口：GET /hello、GET /greet?name=xxx、POST /echo。掌握了 @RestController、@GetMapping、@PostMapping、@RequestParam、@RequestBody 注解的基本用法。理解了 Spring Boot 自动将返回值转为 JSON 的机制。用户还主动询问了 IDEA 快捷键提升效率，已提供速查表参考文档。可以进入路径参数和 DTO 的学习。
